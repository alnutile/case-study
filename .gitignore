# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.env

# Node (MCP server)
node_modules/
dist/

# Local audit DB / runtime data (must never leave Korral's tenancy)
data/
*.db
*.db-journal

# OS / editor
.DS_Store

# Interview prep notes — not a deliverable, keep out of the shared repo
Duvo Case Study/
